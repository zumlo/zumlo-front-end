// API routs
import { environment } from '../../environments/environment';

export const API_SEGMENTS = {
    base: environment.apiUrl,
};

// Will add all the API routes it will help if we need to change the API route then we need to pass on that from this section it will execute on all where we added.
export const API_URLS = {
    contactDetails: 'Utility',

}