// Validations.

export enum validationConstants {
    required = 'required',
    pattern = 'pattern',
    noWhiteSpace = 'noWhiteSpace',
    email = 'email',
    maxLength = 'maxlength',
    maxLengthValue = 10,
    minLength = 'minlength',
    minLengthValue = 0,
    match = 'matched',
}