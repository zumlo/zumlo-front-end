import { ViewPortObserverDirective } from './view-port-observer.directive';

describe('ViewPortObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewPortObserverDirective();
    expect(directive).toBeTruthy();
  });
});
